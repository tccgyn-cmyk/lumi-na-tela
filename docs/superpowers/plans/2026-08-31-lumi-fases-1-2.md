# Lumi na Tela — Plano de Implementação (Fases 1 e 2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Colocar o Lumi (axolote animado) vivo no canto da tela do Windows/Mac, com agendador de intervenções que dispara micro-pausas físicas e exercícios de respiração após tempo de uso ativo do computador.

**Architecture:** App Electron com processo principal (janelas, agendador, monitor de ociosidade) e renderers (Lumi em janela transparente click-through; cartão de atividade em janela flutuante). Lógica do agendador e rodízio em módulos puros CommonJS testáveis com Vitest, sem dependência do Electron. Conteúdo pt-BR em módulo próprio.

**Tech Stack:** Electron ^33, Vitest ^2, JavaScript puro (CommonJS no main, sem framework de UI), SVG + CSS animations para o personagem.

**Escopo:** Fases 1-2 da spec (`docs/superpowers/specs/2026-08-31-lumi-desktop-design.md`). Fora deste plano: check-in de humor, pílulas, painel semanal, configurações persistentes, CTAs, supressão de tela cheia, instaladores (Fases 3-5, planos futuros).

**Nota de conteúdo:** todos os textos pt-BR neste plano são RASCUNHOS no tom da marca — Roberto revisa e aprova antes do lançamento (a revisão não bloqueia a implementação).

---

## Estrutura de arquivos

```
package.json
src/main/main.js              — entrada: orquestra janelas, agendador, IPC, menu
src/main/lumi-window.js       — cria a janela transparente do Lumi
src/main/preload-lumi.js      — ponte IPC da janela do Lumi
src/main/intervention.js      — animação de travessia da janela (andar/voltar)
src/main/scheduler.js         — lógica pura: tempo ativo, disparo, adiar, silenciar
src/main/rotation.js          — lógica pura: rodízio de tipos de atividade
src/main/activity-window.js   — cria a janela do cartão de atividade
src/main/preload-activity.js  — ponte IPC do cartão de atividade
src/shared/content.js         — conteúdo pt-BR: micro-pausas, exercícios, convites
src/renderer/lumi/index.html  — markup do Lumi (SVG) + balão de fala
src/renderer/lumi/lumi.css    — animações (respirar, piscar, andar) e balão
src/renderer/lumi/lumi.js     — estados do personagem, hover, botões do balão
src/renderer/activity/index.html — cartão de atividade
src/renderer/activity/activity.css
src/renderer/activity/activity.js — renderiza micro-pausa ou respiração guiada
tests/scheduler.test.js
tests/rotation.test.js
tests/content.test.js
```

---

### Task 1: Scaffold do projeto

**Files:**
- Create: `package.json`

- [ ] **Step 1: Criar package.json**

```json
{
  "name": "lumi-na-tela",
  "version": "0.1.0",
  "description": "Lumi — seu companheiro de saúde mental na tela (PsicoLabs)",
  "main": "src/main/main.js",
  "author": "Roberto Ribeiro — PsicoLabs",
  "license": "UNLICENSED",
  "scripts": {
    "start": "electron .",
    "test": "vitest run"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: Instalar dependências**

Run: `npm install`
Expected: termina sem erro; `node_modules/` criado (já está no .gitignore).

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: scaffold do projeto Electron + Vitest"
```

---

### Task 2: Janela transparente do Lumi

**Files:**
- Create: `src/main/lumi-window.js`
- Create: `src/main/preload-lumi.js`
- Create: `src/main/main.js`
- Create: `src/renderer/lumi/index.html`

- [ ] **Step 1: Criar lumi-window.js**

```js
const { BrowserWindow, screen } = require('electron');
const path = require('path');

const WIN_W = 260;
const WIN_H = 250;

function homePosition() {
  const { workArea } = screen.getPrimaryDisplay();
  return {
    x: workArea.x + workArea.width - WIN_W - 16,
    y: workArea.y + workArea.height - WIN_H,
  };
}

function createLumiWindow() {
  const { x, y } = homePosition();
  const win = new BrowserWindow({
    width: WIN_W,
    height: WIN_H,
    x,
    y,
    transparent: true,
    frame: false,
    resizable: false,
    movable: false,
    alwaysOnTop: true,
    skipTaskbar: true,
    hasShadow: false,
    focusable: true,
    webPreferences: {
      preload: path.join(__dirname, 'preload-lumi.js'),
    },
  });
  win.setAlwaysOnTop(true, 'screen-saver');
  if (process.platform === 'darwin') {
    win.setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: false });
  }
  win.loadFile(path.join(__dirname, '../renderer/lumi/index.html'));
  win.setIgnoreMouseEvents(true, { forward: true });
  return win;
}

module.exports = { createLumiWindow, homePosition, WIN_W, WIN_H };
```

- [ ] **Step 2: Criar preload-lumi.js**

```js
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('lumiAPI', {
  setIgnoreMouse: (ignore) => ipcRenderer.send('set-ignore-mouse', ignore),
  openMenu: () => ipcRenderer.send('lumi-menu'),
  respond: (answer) => ipcRenderer.send('intervention-response', answer),
  onState: (cb) => ipcRenderer.on('lumi-state', (_e, s) => cb(s)),
});
```

- [ ] **Step 3: Criar main.js (versão inicial)**

```js
const { app, ipcMain } = require('electron');
const { createLumiWindow } = require('./lumi-window');

let lumiWin = null;

const gotLock = app.requestSingleInstanceLock();
if (!gotLock) {
  app.quit();
} else {
  app.whenReady().then(() => {
    lumiWin = createLumiWindow();

    ipcMain.on('set-ignore-mouse', (_e, ignore) => {
      if (lumiWin) lumiWin.setIgnoreMouseEvents(ignore, { forward: true });
    });
  });

  app.on('window-all-closed', () => {
    app.quit();
  });
}
```

- [ ] **Step 4: Criar index.html com placeholder**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <style>
    html, body { margin: 0; background: transparent; overflow: hidden; }
    #placeholder {
      position: absolute; bottom: 8px; right: 8px;
      width: 80px; height: 64px; background: #ffb5c2;
      border-radius: 24px 24px 16px 16px;
    }
  </style>
</head>
<body>
  <div id="placeholder"></div>
</body>
</html>
```

- [ ] **Step 5: Verificação manual**

Run: `npm start`
Expected: um retângulo rosa arredondado aparece no canto inferior direito, flutuando sobre todas as janelas, sem borda nem fundo. Cliques na área transparente passam para o que está atrás. Fechar com Ctrl+C no terminal.

- [ ] **Step 6: Commit**

```bash
git add src/main src/renderer
git commit -m "feat: janela transparente do Lumi no canto da tela"
```

---

### Task 3: Visual do axolote com animações e click-through dinâmico

**Files:**
- Modify: `src/renderer/lumi/index.html` (substituir todo o conteúdo)
- Create: `src/renderer/lumi/lumi.css`
- Create: `src/renderer/lumi/lumi.js`

- [ ] **Step 1: Substituir index.html pelo Lumi completo (SVG + balão)**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="lumi.css" />
</head>
<body>
  <div id="bubble" class="bubble hidden">
    <p id="bubble-text"></p>
    <div id="bubble-actions" class="bubble-actions hidden">
      <button id="btn-accept">Bora! 💪</button>
      <button id="btn-snooze">Daqui a 10 min</button>
      <button id="btn-dismiss">Agora não</button>
    </div>
  </div>
  <div id="lumi" data-state="idle">
    <svg viewBox="0 0 200 170" width="150" height="128" xmlns="http://www.w3.org/2000/svg">
      <g class="body-group">
        <g class="gills gills-left">
          <path d="M70 55 Q55 40 62 30 Q72 42 76 50 Z" fill="#ff8fa8" />
          <path d="M78 45 Q70 25 80 18 Q86 34 88 44 Z" fill="#ff8fa8" />
        </g>
        <g class="gills gills-right">
          <path d="M130 55 Q145 40 138 30 Q128 42 124 50 Z" fill="#ff8fa8" />
          <path d="M122 45 Q130 25 120 18 Q114 34 112 44 Z" fill="#ff8fa8" />
        </g>
        <path class="tail" d="M140 110 Q165 105 160 90" stroke="#ffb5c2" stroke-width="12" fill="none" stroke-linecap="round" />
        <ellipse cx="100" cy="110" rx="48" ry="34" fill="#ffb5c2" />
        <circle cx="100" cy="75" r="34" fill="#ffc4cf" />
        <g class="eyes">
          <circle cx="88" cy="72" r="5" fill="#3a3a3a" />
          <circle cx="112" cy="72" r="5" fill="#3a3a3a" />
          <circle cx="90" cy="70" r="1.5" fill="#fff" />
          <circle cx="114" cy="70" r="1.5" fill="#fff" />
        </g>
        <path class="mouth" d="M90 85 Q100 93 110 85" stroke="#3a3a3a" stroke-width="2.5" fill="none" stroke-linecap="round" />
        <circle cx="74" cy="82" r="5" fill="#ff9db2" opacity="0.7" />
        <circle cx="126" cy="82" r="5" fill="#ff9db2" opacity="0.7" />
      </g>
    </svg>
  </div>
  <script src="lumi.js"></script>
</body>
</html>
```

- [ ] **Step 2: Criar lumi.css**

```css
html, body {
  margin: 0;
  background: transparent;
  overflow: hidden;
  font-family: "Segoe UI", system-ui, sans-serif;
  user-select: none;
  height: 100%;
}

#lumi {
  position: absolute;
  bottom: 0;
  right: 8px;
  cursor: pointer;
  -webkit-app-region: no-drag;
}

/* Respiração calma constante */
.body-group {
  transform-origin: 100px 140px;
  animation: breathe 4.5s ease-in-out infinite;
}
@keyframes breathe {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.035); }
}

/* Piscar de vez em quando */
.eyes {
  transform-origin: 100px 72px;
  animation: blink 6s infinite;
}
@keyframes blink {
  0%, 92%, 100% { transform: scaleY(1); }
  95% { transform: scaleY(0.08); }
}

/* Guelras balançam no hover */
#lumi:hover .gills {
  animation: wiggle 0.6s ease-in-out infinite;
}
.gills-left { transform-origin: 76px 45px; }
.gills-right { transform-origin: 124px 45px; }
@keyframes wiggle {
  0%, 100% { transform: rotate(0deg); }
  50% { transform: rotate(8deg); }
}

/* Andando: balanço mais rápido */
#lumi[data-state="walking"] .body-group {
  animation: bob 0.4s ease-in-out infinite;
}
@keyframes bob {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6px); }
}

/* Balão de fala */
.bubble {
  position: absolute;
  top: 6px;
  left: 8px;
  right: 8px;
  background: #fff;
  border-radius: 14px;
  padding: 10px 12px;
  box-shadow: 0 3px 12px rgba(0, 0, 0, 0.25);
  font-size: 13px;
  color: #3a3a3a;
}
.bubble::after {
  content: "";
  position: absolute;
  bottom: -8px;
  right: 60px;
  border: 8px solid transparent;
  border-top-color: #fff;
  border-bottom: none;
}
.bubble p { margin: 0 0 8px; }
.bubble-actions { display: flex; flex-wrap: wrap; gap: 6px; }
.bubble-actions button {
  border: none;
  border-radius: 10px;
  padding: 6px 10px;
  font-size: 12px;
  cursor: pointer;
  background: #f0edff;
  color: #3a3a3a;
}
.bubble-actions #btn-accept {
  background: #ffb5c2;
  font-weight: 600;
}
.hidden { display: none !important; }
```

- [ ] **Step 3: Criar lumi.js**

```js
const lumiEl = document.getElementById('lumi');
const bubble = document.getElementById('bubble');
const bubbleText = document.getElementById('bubble-text');
const bubbleActions = document.getElementById('bubble-actions');

// Click-through dinâmico: só captura o mouse sobre o Lumi ou o balão
document.addEventListener('mousemove', (e) => {
  const interactive = e.target.closest('#lumi, #bubble');
  window.lumiAPI.setIgnoreMouse(!interactive);
});
document.addEventListener('mouseleave', () => {
  window.lumiAPI.setIgnoreMouse(true);
});

// Menu de contexto no clique direito do Lumi
lumiEl.addEventListener('contextmenu', (e) => {
  e.preventDefault();
  window.lumiAPI.openMenu();
});

// Estados vindos do processo principal
window.lumiAPI.onState((s) => {
  if (s.state) lumiEl.dataset.state = s.state;
  if (s.state === 'invite') {
    bubbleText.textContent = s.message;
    bubble.classList.remove('hidden');
    bubbleActions.classList.remove('hidden');
  } else {
    bubble.classList.add('hidden');
  }
});

document.getElementById('btn-accept').addEventListener('click', () => window.lumiAPI.respond('accept'));
document.getElementById('btn-snooze').addEventListener('click', () => window.lumiAPI.respond('snooze'));
document.getElementById('btn-dismiss').addEventListener('click', () => window.lumiAPI.respond('dismiss'));
```

- [ ] **Step 4: Verificação manual**

Run: `npm start`
Expected: o axolote rosa aparece no canto inferior direito, respirando suavemente e piscando a cada ~6s. Passar o mouse por cima balança as guelras. Cliques fora do Lumi passam para as janelas de trás. O balão não aparece ainda (correto — só na Task 7+).

- [ ] **Step 5: Commit**

```bash
git add src/renderer/lumi
git commit -m "feat: axolote animado com respiracao, piscar, hover e click-through dinamico"
```

---

### Task 4: Scheduler (TDD)

**Files:**
- Create: `tests/scheduler.test.js`
- Create: `src/main/scheduler.js`

- [ ] **Step 1: Escrever os testes que falham**

```js
import { describe, it, expect } from 'vitest';
import { Scheduler } from '../src/main/scheduler.js';

const TICK = 5000; // 5s por tick, como no app real
const MIN = 60_000;

function runTicks(s, { count, startMs = 0, idleSeconds = 0 }) {
  let last = null;
  for (let i = 1; i <= count; i++) {
    last = s.tick(startMs + i * TICK, idleSeconds, TICK);
  }
  return last;
}

describe('Scheduler', () => {
  it('dispara intervencao apos acumular o tempo ativo configurado', () => {
    const s = new Scheduler({ intervalMinutes: 50 });
    // 50 min = 600 ticks de 5s; o último tick deve disparar
    const result = runTicks(s, { count: 600 });
    expect(result).toBe('intervention-due');
  });

  it('nao dispara antes do tempo', () => {
    const s = new Scheduler({ intervalMinutes: 50 });
    const result = runTicks(s, { count: 599 });
    expect(result).toBeNull();
  });

  it('nao acumula tempo enquanto o usuario esta ocioso', () => {
    const s = new Scheduler({ intervalMinutes: 50, idleThresholdSeconds: 300 });
    const result = runTicks(s, { count: 1000, idleSeconds: 600 });
    expect(result).toBeNull();
  });

  it('pausa na ociosidade e retoma de onde parou', () => {
    const s = new Scheduler({ intervalMinutes: 50, idleThresholdSeconds: 300 });
    runTicks(s, { count: 300 });                                  // 25 min ativos
    runTicks(s, { count: 500, startMs: 300 * TICK, idleSeconds: 600 }); // almoço
    const result = runTicks(s, { count: 300, startMs: 800 * TICK });    // +25 min
    expect(result).toBe('intervention-due');
  });

  it('adiar (snooze) segura o disparo ate expirar', () => {
    const s = new Scheduler({ intervalMinutes: 50 });
    runTicks(s, { count: 599 });
    s.snooze(599 * TICK, 10); // adia 10 min
    // Durante o snooze, mesmo passando do limite, não dispara
    const during = runTicks(s, { count: 100, startMs: 599 * TICK });
    expect(during).toBeNull();
    // Depois que o snooze expira, dispara no próximo tick
    const after = s.tick(599 * TICK + 11 * MIN, 0, TICK);
    expect(after).toBe('intervention-due');
  });

  it('silenciar (em atendimento) segura o disparo ate expirar', () => {
    const s = new Scheduler({ intervalMinutes: 50 });
    s.silence(0, 60);
    const during = runTicks(s, { count: 700 });
    expect(during).toBeNull();
    const after = s.tick(61 * MIN + 700 * TICK, 0, TICK);
    expect(after).toBe('intervention-due');
  });

  it('reseta o contador apos disparar', () => {
    const s = new Scheduler({ intervalMinutes: 50 });
    runTicks(s, { count: 600 }); // dispara
    const next = runTicks(s, { count: 599, startMs: 600 * TICK });
    expect(next).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm test`
Expected: FAIL — `Cannot find module '../src/main/scheduler.js'` (ou equivalente).

- [ ] **Step 3: Implementar scheduler.js**

```js
class Scheduler {
  constructor({ intervalMinutes = 50, idleThresholdSeconds = 300 } = {}) {
    this.intervalMs = intervalMinutes * 60_000;
    this.idleThresholdMs = idleThresholdSeconds * 1000;
    this.activeMs = 0;
    this.snoozedUntil = 0;
    this.silencedUntil = 0;
  }

  /**
   * Chamado a cada tick do relógio do app.
   * @param {number} nowMs timestamp atual
   * @param {number} idleSeconds segundos de ociosidade do sistema
   * @param {number} elapsedMs quanto tempo passou desde o último tick
   * @returns {'intervention-due'|null}
   */
  tick(nowMs, idleSeconds, elapsedMs) {
    if (idleSeconds * 1000 >= this.idleThresholdMs) return null;
    this.activeMs += elapsedMs;
    if (nowMs < this.snoozedUntil || nowMs < this.silencedUntil) return null;
    if (this.activeMs >= this.intervalMs) {
      this.activeMs = 0;
      return 'intervention-due';
    }
    return null;
  }

  snooze(nowMs, minutes) {
    this.snoozedUntil = nowMs + minutes * 60_000;
  }

  silence(nowMs, minutes) {
    this.silencedUntil = nowMs + minutes * 60_000;
  }
}

module.exports = { Scheduler };
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm test`
Expected: PASS — 7 testes de `tests/scheduler.test.js` verdes.

- [ ] **Step 5: Commit**

```bash
git add tests/scheduler.test.js src/main/scheduler.js
git commit -m "feat: scheduler de intervencoes com ociosidade, snooze e silencio (TDD)"
```

---

### Task 5: Rodízio de atividades (TDD)

**Files:**
- Create: `tests/rotation.test.js`
- Create: `src/main/rotation.js`

- [ ] **Step 1: Escrever o teste que falha**

```js
import { describe, it, expect } from 'vitest';
import { Rotation } from '../src/main/rotation.js';

describe('Rotation', () => {
  it('alterna os tipos em ciclo', () => {
    const r = new Rotation(['micro-pausa', 'respiracao']);
    expect(r.next()).toBe('micro-pausa');
    expect(r.next()).toBe('respiracao');
    expect(r.next()).toBe('micro-pausa');
    expect(r.next()).toBe('respiracao');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm test`
Expected: FAIL — `Cannot find module '../src/main/rotation.js'`.

- [ ] **Step 3: Implementar rotation.js**

```js
class Rotation {
  constructor(types) {
    this.types = types;
    this.i = 0;
  }

  next() {
    const t = this.types[this.i % this.types.length];
    this.i += 1;
    return t;
  }
}

module.exports = { Rotation };
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm test`
Expected: PASS — todos os testes verdes.

- [ ] **Step 5: Commit**

```bash
git add tests/rotation.test.js src/main/rotation.js
git commit -m "feat: rodizio ciclico de tipos de atividade (TDD)"
```

---

### Task 6: Conteúdo pt-BR (micro-pausas, exercícios, convites)

**Files:**
- Create: `src/shared/content.js`
- Create: `tests/content.test.js`

- [ ] **Step 1: Escrever o teste de validação (falha primeiro)**

```js
import { describe, it, expect } from 'vitest';
import { microPausas, exercicios, convites } from '../src/shared/content.js';

describe('Conteúdo', () => {
  it('tem pelo menos 10 micro-pausas com titulo e texto', () => {
    expect(microPausas.length).toBeGreaterThanOrEqual(10);
    for (const m of microPausas) {
      expect(m.titulo).toBeTruthy();
      expect(m.texto).toBeTruthy();
    }
  });

  it('tem 6 exercicios, cada um com titulo e passos ou respiracao', () => {
    expect(exercicios.length).toBe(6);
    for (const e of exercicios) {
      expect(e.titulo).toBeTruthy();
      expect(Boolean(e.passos) || Boolean(e.respiracao)).toBe(true);
      if (e.respiracao) {
        expect(e.respiracao.inspirar).toBeGreaterThan(0);
        expect(e.respiracao.expirar).toBeGreaterThan(0);
        expect(e.respiracao.ciclos).toBeGreaterThan(0);
      }
    }
  });

  it('tem convites para cada tipo de atividade', () => {
    expect(convites['micro-pausa'].length).toBeGreaterThanOrEqual(3);
    expect(convites['respiracao'].length).toBeGreaterThanOrEqual(3);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm test`
Expected: FAIL — `Cannot find module '../src/shared/content.js'`.

- [ ] **Step 3: Implementar content.js (rascunhos no tom da marca — Roberto revisa depois)**

```js
const microPausas = [
  { titulo: 'Regra 20-20-20', texto: 'A cada 20 minutos, olhe para algo a 20 passos de distância por 20 segundos. Seus olhos agradecem, showww!' },
  { titulo: 'Hora da água', texto: 'Quando foi seu último gole de água? Levanta, enche a garrafa e hidrata esse cérebro que cuida de tanta gente.' },
  { titulo: 'Espreguiça braba', texto: 'Braços pro alto, alonga o corpo inteiro como se tivesse acabado de acordar. Pode fazer barulhinho, ninguém tá julgando.' },
  { titulo: 'Ombros soltos', texto: 'Sobe os ombros até as orelhas, segura 3 segundos... e soltaaa. Repete 3 vezes. A tensão do plantão não merece morar aí.' },
  { titulo: 'Pescoço livre', texto: 'Devagarinho: orelha no ombro direito, depois no esquerdo. 15 segundos de cada lado. Sem pressa, sem dor.' },
  { titulo: 'Caminhadinha', texto: 'Levanta e dá uma volta de 2 minutos. Vale ir até a janela, o corredor ou a cozinha. O prontuário te espera.' },
  { titulo: 'Mãos que trabalham', texto: 'Abre e fecha as mãos 10 vezes, depois gira os punhos. Quem digita e examina o dia todo merece esse carinho.' },
  { titulo: 'Postura de gente', texto: 'Pés no chão, coluna ereta, ombros relaxados. Ajusta a cadeira se precisar. Seu corpo é seu instrumento de trabalho.' },
  { titulo: 'Olhar pro longe', texto: 'Vai até a janela e observa o ponto mais distante que conseguir por 30 segundos. Tela de perto o dia todo cansa demais.' },
  { titulo: 'Respiro de pé', texto: 'Levanta, planta os dois pés no chão e respira fundo 3 vezes de olhos abertos. Pronto: recalibrou.' },
];

const exercicios = [
  {
    id: 'diafragmatica',
    titulo: 'Respiração diafragmática',
    descricao: 'Mão na barriga. Vamos respirar fundo juntos — o Lumi infla com você.',
    respiracao: { inspirar: 4, segurar: 0, expirar: 6, pausa: 0, ciclos: 6 },
  },
  {
    id: '478',
    titulo: 'Respiração 4-7-8',
    descricao: 'Inspira em 4, segura em 7, solta em 8. Ótima pra desacelerar depois de um caso difícil.',
    respiracao: { inspirar: 4, segurar: 7, expirar: 8, pausa: 0, ciclos: 4 },
  },
  {
    id: 'grounding',
    titulo: 'Grounding 5-4-3-2-1',
    descricao: 'Um exercício pra voltar pro aqui e agora quando a cabeça tá a mil.',
    passos: [
      'Note 5 coisas que você consegue VER ao seu redor.',
      'Note 4 coisas que você consegue TOCAR agora.',
      'Note 3 sons que você consegue OUVIR.',
      'Note 2 cheiros que você consegue SENTIR.',
      'Note 1 sabor na sua boca.',
      'Respira fundo. Você está aqui, agora. 💛',
    ],
  },
  {
    id: 'defusao',
    titulo: 'Nomeie o pensamento (ACT)',
    descricao: 'Um truque da ACT pra dar um passo atrás dos pensamentos difíceis.',
    passos: [
      'Perceba o pensamento que está te incomodando agora.',
      'Agora reformule: "Estou tendo o pensamento de que..." e complete.',
      'Mais um passo: "Eu percebo que estou tendo o pensamento de que..."',
      'Sentiu a distância? Você não É o pensamento — você o observa.',
    ],
  },
  {
    id: 'escaneamento',
    titulo: 'Escaneamento corporal rápido',
    descricao: 'Um minuto pra checar como seu corpo está carregando o dia.',
    passos: [
      'Feche os olhos ou suavize o olhar.',
      'Leve a atenção pro topo da cabeça... testa... mandíbula. Solta a mandíbula.',
      'Desça pelos ombros... braços... mãos. Onde tem tensão, respira pra lá.',
      'Peito... barriga... pernas... pés no chão.',
      'Abra os olhos. Como está seu corpo agora?',
    ],
  },
  {
    id: 'pausa-visual',
    titulo: 'Pausa 20-20-20 guiada',
    descricao: 'Descanso ativo pros seus olhos, guiado pelo Lumi.',
    passos: [
      'Olhe para o ponto mais distante que conseguir (janela ajuda!).',
      'Mantenha o olhar lá por 20 segundos, piscando naturalmente.',
      'Agora feche os olhos por 5 segundos.',
      'Prontinho! Seus olhos renovados pra próxima rodada.',
    ],
  },
];

const convites = {
  'micro-pausa': [
    'Ei! Você tá firme aí há um tempão. Bora dar uma pausinha?',
    'Psiu! Seu corpo tá pedindo um alongamento. Topa?',
    'Quem cuida de todo mundo também precisa de pausa. Vem comigo?',
    'Hora de levantar dessa cadeira, vai por mim. Bora?',
  ],
  respiracao: [
    'Que tal respirar fundo comigo? Só uns minutinhos.',
    'Bora acalmar a mente um pouquinho? Eu respiro junto!',
    'Momento Lumi: uma respirada pra recarregar. Topa?',
    'Pausa pra respirar? Eu te guio, é rapidinho.',
  ],
};

module.exports = { microPausas, exercicios, convites };
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npm test`
Expected: PASS — todos os testes verdes.

- [ ] **Step 5: Commit**

```bash
git add src/shared/content.js tests/content.test.js
git commit -m "feat: conteudo pt-BR de micro-pausas, exercicios e convites (rascunho para revisao)"
```

---

### Task 7: Ligar o cérebro — monitor de ociosidade, agendador e menu do Lumi

**Files:**
- Modify: `src/main/main.js` (substituir todo o conteúdo)

- [ ] **Step 1: Substituir main.js pela versão com agendador e menu**

```js
const { app, ipcMain, Menu, powerMonitor } = require('electron');
const { createLumiWindow } = require('./lumi-window');
const { Scheduler } = require('./scheduler');
const { Rotation } = require('./rotation');

const TICK_MS = 5000;

// LUMI_DEV_INTERVAL=1 npm start → intervalo de 1 min para testar
const intervalMinutes = process.env.LUMI_DEV_INTERVAL
  ? Number(process.env.LUMI_DEV_INTERVAL)
  : 50;

let lumiWin = null;
const scheduler = new Scheduler({ intervalMinutes });
const rotation = new Rotation(['micro-pausa', 'respiracao']);

const gotLock = app.requestSingleInstanceLock();
if (!gotLock) {
  app.quit();
} else {
  app.whenReady().then(() => {
    lumiWin = createLumiWindow();

    ipcMain.on('set-ignore-mouse', (_e, ignore) => {
      if (lumiWin) lumiWin.setIgnoreMouseEvents(ignore, { forward: true });
    });

    ipcMain.on('lumi-menu', () => {
      const menu = Menu.buildFromTemplate([
        { label: 'Pausar agora', click: () => triggerIntervention() },
        { type: 'separator' },
        {
          label: 'Em atendimento',
          submenu: [30, 60, 120].map((min) => ({
            label: `${min} minutos`,
            click: () => scheduler.silence(Date.now(), min),
          })),
        },
        { type: 'separator' },
        { label: 'Sair do Lumi', click: () => app.quit() },
      ]);
      menu.popup({ window: lumiWin });
    });

    setInterval(() => {
      const due = scheduler.tick(
        Date.now(),
        powerMonitor.getSystemIdleTime(),
        TICK_MS
      );
      if (due) triggerIntervention();
    }, TICK_MS);
  });

  app.on('window-all-closed', () => {
    app.quit();
  });
}

function triggerIntervention() {
  const tipo = rotation.next();
  // Na Task 8 isto vira a travessia + balão. Por enquanto, log:
  console.log(`[lumi] intervenção devida: ${tipo}`);
}
```

- [ ] **Step 2: Verificação manual**

Run (PowerShell): `$env:LUMI_DEV_INTERVAL='1'; npm start`
Expected: após ~1 minuto mexendo no computador, o terminal mostra `[lumi] intervenção devida: micro-pausa`. Clique direito no Lumi abre o menu com "Pausar agora", "Em atendimento" e "Sair do Lumi". "Pausar agora" imprime a intervenção na hora. "Sair do Lumi" fecha o app.

- [ ] **Step 3: Commit**

```bash
git add src/main/main.js
git commit -m "feat: agendador ligado ao monitor de ociosidade + menu de contexto do Lumi"
```

---

### Task 8: Travessia da tela e balão de convite

**Files:**
- Create: `src/main/intervention.js`
- Modify: `src/main/main.js` (substituir todo o conteúdo)

- [ ] **Step 1: Criar intervention.js**

```js
const { screen } = require('electron');
const { WIN_W, homePosition } = require('./lumi-window');

function animateTo(win, targetX, durationMs, done) {
  const [startX, y] = win.getPosition();
  const steps = Math.max(1, Math.round(durationMs / 16));
  let i = 0;
  const timer = setInterval(() => {
    i += 1;
    const x = Math.round(startX + (targetX - startX) * (i / steps));
    if (win.isDestroyed()) {
      clearInterval(timer);
      return;
    }
    win.setPosition(x, y);
    if (i >= steps) {
      clearInterval(timer);
      if (done) done();
    }
  }, 16);
}

// Lumi nada até o centro da tela e faz o convite
function walkToCenter(win, message, tipo) {
  const { workArea } = screen.getPrimaryDisplay();
  const targetX = workArea.x + Math.round(workArea.width / 2 - WIN_W / 2);
  win.webContents.send('lumi-state', { state: 'walking' });
  animateTo(win, targetX, 1800, () => {
    win.webContents.send('lumi-state', { state: 'invite', message, tipo });
  });
}

// Lumi volta pro cantinho dele
function returnHome(win) {
  const { x } = homePosition();
  win.webContents.send('lumi-state', { state: 'walking' });
  animateTo(win, x, 1800, () => {
    win.webContents.send('lumi-state', { state: 'idle' });
  });
}

module.exports = { walkToCenter, returnHome };
```

- [ ] **Step 2: Substituir main.js integrando a travessia**

```js
const { app, ipcMain, Menu, powerMonitor } = require('electron');
const { createLumiWindow } = require('./lumi-window');
const { Scheduler } = require('./scheduler');
const { Rotation } = require('./rotation');
const { walkToCenter, returnHome } = require('./intervention');
const { convites } = require('../shared/content');

const TICK_MS = 5000;
const INVITE_TIMEOUT_MS = 2 * 60_000;

const intervalMinutes = process.env.LUMI_DEV_INTERVAL
  ? Number(process.env.LUMI_DEV_INTERVAL)
  : 50;

let lumiWin = null;
let currentTipo = null;
let inviteTimeout = null;
const scheduler = new Scheduler({ intervalMinutes });
const rotation = new Rotation(['micro-pausa', 'respiracao']);

const gotLock = app.requestSingleInstanceLock();
if (!gotLock) {
  app.quit();
} else {
  app.whenReady().then(() => {
    lumiWin = createLumiWindow();

    ipcMain.on('set-ignore-mouse', (_e, ignore) => {
      if (lumiWin) lumiWin.setIgnoreMouseEvents(ignore, { forward: true });
    });

    ipcMain.on('lumi-menu', () => {
      const menu = Menu.buildFromTemplate([
        { label: 'Pausar agora', click: () => triggerIntervention() },
        { type: 'separator' },
        {
          label: 'Em atendimento',
          submenu: [30, 60, 120].map((min) => ({
            label: `${min} minutos`,
            click: () => scheduler.silence(Date.now(), min),
          })),
        },
        { type: 'separator' },
        { label: 'Sair do Lumi', click: () => app.quit() },
      ]);
      menu.popup({ window: lumiWin });
    });

    ipcMain.on('intervention-response', (_e, answer) => {
      handleResponse(answer);
    });

    setInterval(() => {
      const due = scheduler.tick(
        Date.now(),
        powerMonitor.getSystemIdleTime(),
        TICK_MS
      );
      if (due) triggerIntervention();
    }, TICK_MS);
  });

  app.on('window-all-closed', () => {
    app.quit();
  });
}

function pick(list) {
  return list[Math.floor(Math.random() * list.length)];
}

function triggerIntervention() {
  if (currentTipo) return; // já tem convite na tela
  currentTipo = rotation.next();
  walkToCenter(lumiWin, pick(convites[currentTipo]), currentTipo);
  inviteTimeout = setTimeout(() => handleResponse('dismiss'), INVITE_TIMEOUT_MS);
}

function handleResponse(answer) {
  if (!currentTipo) return;
  const tipo = currentTipo;
  currentTipo = null;
  clearTimeout(inviteTimeout);
  returnHome(lumiWin);
  if (answer === 'snooze') {
    scheduler.snooze(Date.now(), 10);
  }
  if (answer === 'accept') {
    // Na Task 9 isto abre o cartão de atividade. Por enquanto, log:
    console.log(`[lumi] atividade aceita: ${tipo}`);
  }
}
```

- [ ] **Step 3: Verificação manual**

Run (PowerShell): `$env:LUMI_DEV_INTERVAL='1'; npm start`
Expected: após 1 min de uso (ou "Pausar agora" no clique direito), o Lumi atravessa a tela balançando até o centro inferior e mostra o balão com convite + 3 botões. "Agora não" e "Daqui a 10 min" fazem o Lumi voltar pro canto; "Bora!" volta e imprime `[lumi] atividade aceita: ...` no terminal. Sem resposta por 2 min, ele volta sozinho.

- [ ] **Step 4: Commit**

```bash
git add src/main/intervention.js src/main/main.js
git commit -m "feat: travessia da tela com balao de convite (aceitar/adiar/dispensar)"
```

---

### Task 9: Cartão de atividade (micro-pausa e respiração guiada)

**Files:**
- Create: `src/main/activity-window.js`
- Create: `src/main/preload-activity.js`
- Create: `src/renderer/activity/index.html`
- Create: `src/renderer/activity/activity.css`
- Create: `src/renderer/activity/activity.js`
- Modify: `src/main/main.js:handleResponse` (trocar o log pelo cartão)

- [ ] **Step 1: Criar activity-window.js**

```js
const { BrowserWindow, ipcMain } = require('electron');
const path = require('path');
const { microPausas, exercicios } = require('../shared/content');

function pick(list) {
  return list[Math.floor(Math.random() * list.length)];
}

function pickContent(tipo) {
  if (tipo === 'micro-pausa') {
    return { tipo, ...pick(microPausas) };
  }
  return { tipo, ...pick(exercicios) };
}

function openActivity(tipo) {
  const item = pickContent(tipo);
  const win = new BrowserWindow({
    width: 400,
    height: 480,
    frame: false,
    resizable: false,
    alwaysOnTop: true,
    skipTaskbar: true,
    webPreferences: {
      preload: path.join(__dirname, 'preload-activity.js'),
    },
  });
  win.loadFile(path.join(__dirname, '../renderer/activity/index.html'));
  win.webContents.on('did-finish-load', () => {
    win.webContents.send('activity-data', item);
  });
  return win;
}

ipcMain.on('activity-done', (e) => {
  const win = BrowserWindow.fromWebContents(e.sender);
  if (win && !win.isDestroyed()) win.close();
});

module.exports = { openActivity };
```

- [ ] **Step 2: Criar preload-activity.js**

```js
const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('activityAPI', {
  onData: (cb) => ipcRenderer.on('activity-data', (_e, item) => cb(item)),
  done: () => ipcRenderer.send('activity-done'),
});
```

- [ ] **Step 3: Criar index.html do cartão**

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <link rel="stylesheet" href="activity.css" />
</head>
<body>
  <div class="card">
    <h1 id="titulo"></h1>
    <p id="descricao"></p>
    <div id="breath-area" class="hidden">
      <div id="circle" class="circle"></div>
      <p id="breath-label"></p>
    </div>
    <ol id="passos" class="hidden"></ol>
    <p id="texto" class="hidden"></p>
    <button id="btn-done">Concluído ✨</button>
  </div>
  <script src="activity.js"></script>
</body>
</html>
```

- [ ] **Step 4: Criar activity.css**

```css
html, body {
  margin: 0;
  font-family: "Segoe UI", system-ui, sans-serif;
  background: #fff7f9;
  color: #3a3a3a;
  user-select: none;
}

.card {
  padding: 24px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  text-align: center;
  min-height: 100vh;
  box-sizing: border-box;
  justify-content: center;
}

h1 { font-size: 20px; margin: 0; color: #d4567a; }
p { margin: 0; font-size: 14px; line-height: 1.5; }

#breath-area {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 12px 0;
}

.circle {
  width: 90px;
  height: 90px;
  border-radius: 50%;
  background: radial-gradient(circle at 35% 35%, #ffd0da, #ffb5c2);
  transform: scale(1);
  transition-property: transform;
  transition-timing-function: ease-in-out;
}
.circle.grow { transform: scale(1.6); }
.circle.shrink { transform: scale(1); }

#breath-label { font-size: 16px; font-weight: 600; color: #d4567a; }

ol { text-align: left; font-size: 14px; line-height: 1.7; padding-left: 22px; margin: 0; }

#btn-done {
  margin-top: 8px;
  border: none;
  border-radius: 12px;
  padding: 10px 22px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  background: #ffb5c2;
  color: #3a3a3a;
}

.hidden { display: none !important; }
```

- [ ] **Step 5: Criar activity.js**

```js
const tituloEl = document.getElementById('titulo');
const descricaoEl = document.getElementById('descricao');
const breathArea = document.getElementById('breath-area');
const circle = document.getElementById('circle');
const breathLabel = document.getElementById('breath-label');
const passosEl = document.getElementById('passos');
const textoEl = document.getElementById('texto');

window.activityAPI.onData((item) => {
  tituloEl.textContent = item.titulo;
  if (item.tipo === 'micro-pausa') {
    textoEl.textContent = item.texto;
    textoEl.classList.remove('hidden');
    return;
  }
  descricaoEl.textContent = item.descricao || '';
  if (item.respiracao) {
    breathArea.classList.remove('hidden');
    runBreath(item.respiracao);
  } else if (item.passos) {
    passosEl.classList.remove('hidden');
    for (const passo of item.passos) {
      const li = document.createElement('li');
      li.textContent = passo;
      passosEl.appendChild(li);
    }
  }
});

function runBreath(r) {
  const fases = [
    ['Inspira...', r.inspirar, 'grow'],
    ['Segura', r.segurar, 'grow'],
    ['Solta...', r.expirar, 'shrink'],
    ['Pausa', r.pausa, 'shrink'],
  ].filter((f) => f[1] > 0);

  let ciclo = 0;
  let i = 0;

  function step() {
    if (ciclo >= r.ciclos) {
      breathLabel.textContent = 'Prontinho! Showww 💛';
      circle.className = 'circle shrink';
      return;
    }
    const [texto, segundos, classe] = fases[i];
    breathLabel.textContent = `${texto} (${segundos}s)`;
    circle.style.transitionDuration = `${segundos}s`;
    circle.className = `circle ${classe}`;
    setTimeout(() => {
      i = (i + 1) % fases.length;
      if (i === 0) ciclo += 1;
      step();
    }, segundos * 1000);
  }
  step();
}

document.getElementById('btn-done').addEventListener('click', () => {
  window.activityAPI.done();
});
```

- [ ] **Step 6: Ligar no main.js**

Em `src/main/main.js`, adicionar o require no topo, junto aos outros:

```js
const { openActivity } = require('./activity-window');
```

E em `handleResponse`, substituir o bloco do `accept`:

```js
  if (answer === 'accept') {
    openActivity(tipo);
  }
```

(Remover a linha do `console.log`.)

- [ ] **Step 7: Verificação manual**

Run (PowerShell): `$env:LUMI_DEV_INTERVAL='1'; npm start`
Expected: aceitar um convite de micro-pausa abre um cartão rosa-claro centralizado com título + texto + botão "Concluído ✨" que fecha o cartão. Aceitar um convite de respiração abre o cartão com o círculo que cresce e encolhe no ritmo (ex.: diafragmática 4s/6s, 6 ciclos) e a legenda "Inspira... / Solta...", terminando em "Prontinho! Showww 💛". Exercícios com passos (grounding, defusão) mostram a lista numerada.

- [ ] **Step 8: Rodar os testes de regressão**

Run: `npm test`
Expected: PASS — todos os testes continuam verdes.

- [ ] **Step 9: Commit**

```bash
git add src/main/activity-window.js src/main/preload-activity.js src/renderer/activity src/main/main.js
git commit -m "feat: cartao de atividade com micro-pausa e respiracao guiada animada"
```

---

### Task 10: Roteiro de teste manual das Fases 1-2

**Files:**
- Create: `docs/superpowers/teste-manual-fases-1-2.md`

- [ ] **Step 1: Criar o roteiro de teste manual**

```markdown
# Roteiro de teste manual — Fases 1-2

Rodar antes de considerar as Fases 1-2 concluídas. Usar `$env:LUMI_DEV_INTERVAL='1'; npm start` para acelerar.

## Lumi vivo
- [ ] Lumi aparece no canto inferior direito, sobre todas as janelas
- [ ] Respira suavemente e pisca de tempos em tempos
- [ ] Hover balança as guelras
- [ ] Cliques FORA do Lumi passam para a janela de trás (testar clicando num texto atrás dele)
- [ ] Clique direito no Lumi abre o menu (Pausar agora / Em atendimento / Sair)

## Cérebro
- [ ] Após 1 min de uso ativo, o Lumi atravessa a tela e convida
- [ ] Ficar 5+ min sem tocar no computador NÃO conta como tempo ativo (esperar e confirmar que o convite demora mais)
- [ ] "Em atendimento 30 min" segura os convites
- [ ] "Daqui a 10 min" adia e o convite volta depois
- [ ] Sem resposta por 2 min, o Lumi volta sozinho pro canto

## Atividades
- [ ] Micro-pausa: cartão com texto + Concluído fecha
- [ ] Respiração diafragmática: círculo anima no ritmo 4s/6s por 6 ciclos
- [ ] Exercício com passos (grounding/defusão): lista numerada legível
- [ ] Rodízio alterna micro-pausa ↔ respiração

## Robustez
- [ ] `npm test` passa
- [ ] App usa < 5% CPU em idle (conferir no Gerenciador de Tarefas)
- [ ] Fechar pelo menu "Sair do Lumi" encerra o processo completamente
```

- [ ] **Step 2: Executar o roteiro completo**

Run: seguir o roteiro acima manualmente.
Expected: todos os itens marcados. Qualquer falha vira correção antes do commit final.

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/teste-manual-fases-1-2.md
git commit -m "docs: roteiro de teste manual das fases 1-2"
```

---

## Próximos planos (fora deste)

- **Fases 3:** onboarding, check-in de humor, pílulas de psicoeducação, painel semanal, configurações persistentes, supressão de tela cheia.
- **Fase 4:** CTAs do funil, imagem de compartilhamento, conteúdo remoto atualizável.
- **Fase 5:** instaladores Win/Mac, auto-update, beta, assinatura de código.
